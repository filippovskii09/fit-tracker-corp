# Agent Instructions

This repository uses `.agents` as the canonical source of truth for AI-assisted development.

Start here:

1. Read `.agents/README.md`.
2. Read `.agents/skills/common-agent-rules/SKILL.md`.
3. Load only the task-specific skill needed for the current request.

Do not copy or expand rules in this file. Update `.agents` when durable workflow guidance changes.
