import {
  authLocale,
  calendarLocale,
  commonLocale,
  modalsLocales,
  workoutLocales,
} from './en';

export const DICTIONARY = {
  auth: authLocale,
  common: commonLocale,
  calendar: calendarLocale,
  modals: modalsLocales,
  workout: workoutLocales,
} as const;
