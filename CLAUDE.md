# Claude Instructions

`.agents` is the canonical AI workflow layer for this repository.

Before working:

1. Read `.agents/README.md`.
2. Read `.agents/skills/common-agent-rules/SKILL.md`.
3. Use task-specific skills from `.agents/skills` when relevant.

Keep this file as a thin Claude-oriented adapter. Durable rules belong in `.agents`.
