# Gemini Instructions

Use `.agents` as the canonical project context.

Before analysis, implementation, or review:

1. Read `.agents/README.md`.
2. Read `.agents/skills/common-agent-rules/SKILL.md`.
3. Use the relevant task skill from `.agents/skills`.

Do not duplicate full rules here. If guidance must persist across tools, update `.agents`.
